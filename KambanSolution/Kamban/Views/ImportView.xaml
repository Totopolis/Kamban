<UserControl x:Class="Kamban.Views.ImportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ScrollViewer>
            <StackPanel Orientation="Vertical" Margin="20" MaxWidth="600">
                <TextBlock FontWeight="DemiBold" Margin="0,0,0,5">Redmine</TextBlock>
                <Button Command="{Binding ImportRedmineCommand}">Login to Redmine</Button>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
